const MARKER="\u2063";const input=document.getElementById("input");const generateBtn=document.getElementById("generateBtn");const inputSection=document.getElementById("inputSection");const testSection=document.getElementById("testSection");const questionsContainer=document.getElementById("questions");const checkBtn=document.getElementById("checkBtn");const resetBtn=document.getElementById("resetBtn");const newTestBtn=document.getElementById("newTestBtn");const resultSection=document.getElementById("resultSection");const correctCount=document.getElementById("correctCount");const wrongCount=document.getElementById("wrongCount");const skippedCount=document.getElementById("skippedCount");const percentCount=document.getElementById("percentCount");const mistakesContainer=document.getElementById("mistakes");const progressBar=document.getElementById("progressBar");const timerElement=document.getElementById("timer");const shuffleQuestions=document.getElementById("shuffleQuestions");const shuffleAnswers=document.getElementById("shuffleAnswers");const questionNav=document.getElementById("questionNav");const clearInputBtn=document.getElementById("clearInputBtn");function updateClearBtn(){if(input.value.trim()){clearInputBtn.classList.remove("hidden")}else{clearInputBtn.classList.add("hidden")}}
input.addEventListener("input",updateClearBtn);updateClearBtn();clearInputBtn.addEventListener("click",()=>{input.value="";localStorage.removeItem("blindtest");clearInputBtn.classList.add("hidden");input.focus()});const copyPromptBtn=document.getElementById("copyPromptBtn");const copyPromptIcon=document.getElementById("copyPromptIcon");const copyPromptText=document.getElementById("copyPromptText");const aiPrompt=document.getElementById("aiPrompt");copyPromptBtn.addEventListener("click",async()=>{try{await navigator.clipboard.writeText(aiPrompt.textContent.trim());copyPromptBtn.classList.add("copied");copyPromptIcon.querySelector("use").setAttribute("href","#icon-check");copyPromptText.textContent="Скопировано";setTimeout(()=>{copyPromptBtn.classList.remove("copied");copyPromptIcon.querySelector("use").setAttribute("href","#icon-copy");copyPromptText.textContent="Скопировать"},2000)}catch{const range=document.createRange();range.selectNode(aiPrompt);window.getSelection().removeAllRanges();window.getSelection().addRange(range);document.execCommand("copy");window.getSelection().removeAllRanges();copyPromptText.textContent="Скопировано";setTimeout(()=>{copyPromptText.textContent="Скопировать"},2000)}});let testActive=!1;window.addEventListener("beforeunload",(e)=>{if(testActive){e.preventDefault();e.returnValue=""}});input.value=localStorage.getItem("blindtest")||"";input.addEventListener("input",()=>{localStorage.setItem("blindtest",input.value)});const themeBtn=document.getElementById("themeBtn");const themeBtnIcon=document.getElementById("themeBtnIcon");themeBtn.addEventListener("click",()=>{document.body.classList.toggle("light");if(document.body.classList.contains("light")){document.documentElement.style.setProperty("--bg","#edf2ff");document.documentElement.style.setProperty("--text","#111");document.documentElement.style.setProperty("--card","rgba(255,255,255,0.7)");themeBtnIcon.querySelector("use").setAttribute("href","#icon-sun")}else{document.documentElement.style.setProperty("--bg","#0d111c");document.documentElement.style.setProperty("--text","#fff");document.documentElement.style.setProperty("--card","rgba(255,255,255,0.06)");themeBtnIcon.querySelector("use").setAttribute("href","#icon-moon")}});const scrollTopBtn=document.getElementById("scrollTopBtn");window.addEventListener("scroll",()=>{if(window.scrollY>300){scrollTopBtn.classList.add("visible")}else{scrollTopBtn.classList.remove("visible")}},{passive:!0});scrollTopBtn.addEventListener("click",()=>{window.scrollTo({top:0,behavior:"smooth"})});const navToggleBtn=document.getElementById("navToggleBtn");const navDrawer=document.getElementById("navDrawer");const navDrawerOverlay=document.getElementById("navDrawerOverlay");const navDrawerClose=document.getElementById("navDrawerClose");const navDrawerGrid=document.getElementById("navDrawerGrid");const drawerProgressBar=document.getElementById("drawerProgressBar");function openDrawer(){navDrawer.classList.add("open");navDrawerOverlay.style.display="block";requestAnimationFrame(()=>navDrawerOverlay.classList.add("open"))}
function closeDrawer(){navDrawer.classList.remove("open");navDrawerOverlay.classList.remove("open");setTimeout(()=>{navDrawerOverlay.style.display="none"},260)}
navToggleBtn.addEventListener("click",()=>{navDrawer.classList.contains("open")?closeDrawer():openDrawer()});navDrawerClose.addEventListener("click",closeDrawer);navDrawerOverlay.addEventListener("click",closeDrawer);let seconds=0;let interval;function startTimer(){clearInterval(interval);seconds=0;interval=setInterval(()=>{seconds++;const minutes=String(Math.floor(seconds/60)).padStart(2,"0");const sec=String(seconds%60).padStart(2,"0");timerElement.textContent=`${minutes}:${sec}`},1000)}
function shuffle(arr){for(let i=arr.length-1;i>0;i--){const j=Math.floor(Math.random()*(i+1));[arr[i],arr[j]]=[arr[j],arr[i]]}
return arr}
function parseQuestions(text){const lines=text.split("\n").map(l=>l.trim());const questions=[];let current=null;for(const line of lines){if(!line)continue;if(/^\d+\./.test(line)){if(current){questions.push(current)}
current={question:line.replace(/^\d+\.\s*/,""),answers:[]};continue}
if(/^[A-D]\)/.test(line)){const isCorrect=line.includes(MARKER);const clean=line.replaceAll(MARKER,"").trim();current.answers.push({text:clean,correct:isCorrect});continue}
if(current){current.question+="\n"+line}}
if(current){questions.push(current)}
return questions}
let questions=[];generateBtn.addEventListener("click",()=>{const text=input.value.trim();if(!text){alert("Вставь тест");return}
questions=parseQuestions(text);if(!questions.length){alert("Ошибка parsing");return}
if(shuffleQuestions.checked){shuffle(questions)}
if(shuffleAnswers.checked){questions.forEach(q=>{shuffle(q.answers)})}
inputSection.classList.add("hidden");testSection.classList.remove("hidden");testActive=!0;renderQuestions();createQuestionNav();startTimer()});function renderQuestions(){questionsContainer.innerHTML="";questions.forEach((q,qIndex)=>{const card=document.createElement("div");card.className="glass question-card";card.innerHTML=`

      <div class="question-title">${qIndex + 1}. ${q.question}</div>

      <div class="answers">

        ${q.answers.map((a, aIndex) => `<label class="answer"><input
type="radio"
name="q-${qIndex}"
value="${aIndex}">${a.text}</label>`).join("")}

      </div>
    `;questionsContainer.appendChild(card)})}
function createQuestionNav(){questionNav.innerHTML="";navDrawerGrid.innerHTML="";questions.forEach((_,index)=>{const item=document.createElement("div");item.className="nav-item";item.textContent=index+1;item.addEventListener("click",()=>{document.querySelectorAll(".question-card")[index].scrollIntoView({behavior:"smooth"})});questionNav.appendChild(item);const drawerItem=document.createElement("div");drawerItem.className="nav-item";drawerItem.textContent=index+1;drawerItem.addEventListener("click",()=>{closeDrawer();setTimeout(()=>{document.querySelectorAll(".question-card")[index].scrollIntoView({behavior:"smooth"})},280)});navDrawerGrid.appendChild(drawerItem)})}
document.addEventListener("change",updateProgress);function updateProgress(){let answered=0;const navItems=document.querySelectorAll("#questionNav .nav-item");const drawerNavItems=document.querySelectorAll("#navDrawerGrid .nav-item");questions.forEach((_,index)=>{const checked=document.querySelector(`input[name="q-${index}"]:checked`);if(checked){answered++;navItems[index]?.classList.add("answered");drawerNavItems[index]?.classList.add("answered")}else{navItems[index]?.classList.remove("answered");drawerNavItems[index]?.classList.remove("answered")}});const percent=(answered/questions.length)*100;progressBar.style.width=`${percent}%`;if(drawerProgressBar)drawerProgressBar.style.width=`${percent}%`}
function fullReset(){clearInterval(interval);testActive=!1;questions=[];questionsContainer.innerHTML="";questionNav.innerHTML="";navDrawerGrid.innerHTML="";mistakesContainer.innerHTML="";progressBar.style.width="0%";if(drawerProgressBar)drawerProgressBar.style.width="0%";timerElement.textContent="00:00";testSection.classList.add("hidden");resultSection.classList.add("hidden");inputSection.classList.remove("hidden");window.scrollTo({top:0,behavior:"smooth"})}
newTestBtn.addEventListener("click",()=>{if(!confirm("Вернуться к вводу теста? Все ответы будут потеряны."))return;fullReset()});resetBtn.addEventListener("click",()=>{if(!confirm("Сбросить все ответы?"))return;document.querySelectorAll('input[type="radio"]').forEach(r=>{r.checked=!1});document.querySelectorAll(".answer").forEach(a=>{a.classList.remove("correct","wrong")});document.querySelectorAll(".nav-item").forEach(n=>{n.classList.remove("answered")});progressBar.style.width="0%";if(drawerProgressBar)drawerProgressBar.style.width="0%";resultSection.classList.add("hidden");mistakesContainer.innerHTML="";startTimer();window.scrollTo({top:0,behavior:"smooth"})});checkBtn.addEventListener("click",()=>{clearInterval(interval);testActive=!1;let correct=0;let wrong=0;let skipped=0;mistakesContainer.innerHTML="";questions.forEach((q,qIndex)=>{const selected=document.querySelector(`input[name="q-${qIndex}"]:checked`);const radios=document.querySelectorAll(`input[name="q-${qIndex}"]`);radios.forEach((radio,index)=>{const label=radio.parentElement;if(q.answers[index].correct){label.classList.add("correct")}});if(!selected){skipped++;return}
const answerIndex=Number(selected.value);if(q.answers[answerIndex].correct){correct++}else{wrong++;selected.parentElement.classList.add("wrong");const correctAnswer=q.answers.find(a=>a.correct);const div=document.createElement("div");div.className="mistake";div.innerHTML=`
        <strong>Вопрос</strong>
        <p>${q.question}</p>
        <strong style="margin-top:8px;display:block;">Правильный ответ</strong>
        <p class="correct-answer-text">${correctAnswer.text}</p>
      `;mistakesContainer.appendChild(div)}});const total=questions.length;const percent=Math.round((correct/total)*100);correctCount.textContent=correct;wrongCount.textContent=wrong;skippedCount.textContent=skipped;percentCount.textContent=`${percent}%`;if(wrong>0){const title=document.createElement("div");title.className="mistakes-title";title.textContent=`Разбор ошибок (${wrong})`;mistakesContainer.prepend(title)}
resultSection.classList.remove("hidden");resultSection.scrollIntoView({behavior:"smooth"})})